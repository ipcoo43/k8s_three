apiVersion: v1
kind: Service
metadata:
  name: svc-clusterip
spec:
  type: ClusterIP
  clusterIP: 10.100.10.10
  selector:
    app: svc-clusterip
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080